version: '3.4'

services:
  catalogdb:
    image: mongo
  basketdb:
    image: redis:alpine
  catalog_api:
    image: ramane/catalog_api    
  basket_api:
    image: ramane/basket_api

volumes:
  mongo_data: